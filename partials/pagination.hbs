<nav class='pagination' role='navigation' aria-label='pagination'>
    {{#if prev}}
        <a href='{{page url prev}}' class='pagination-button pagination-next'>
            Newer Episodes
        </a>
    {{/if}}

    <span class='pagination-link is-current'>
        Page {{page}} of {{pages}}
    </span>

    {{#if next}}
        <a
            href='{{page url next}}'
            class='pagination-button pagination-previous'
        >
            Older Episodes
        </a>
    {{/if}}
</nav>